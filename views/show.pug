extends layout

block content
  h1= article.title
  h5 Written by #{article.author}
  p= article.body
  hr
  if user && user.name === article.author
    a.btn.btn-primary(href="/articles/"+article.id+"/edit") Edit
    a.btn.btn-danger.delete-article(href="#", data-id=article._id) Delete